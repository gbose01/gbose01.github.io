# 2026-05-17

Started the session. The user requested a quick audit of the entire site to remove any 'em-dashes' and replace them with commas.
We audited the site and found em-dashes in the following files:
- `index.html`: date ranges (e.g. "2024 — Present") and text descriptions.
- `blog/building-grove-keepers.html`: text description.
- `blog/mynt-nft.md`: draft post text and list descriptions. (Note: the actual HTML `blog/mynt-nft.html` has already been cleaned up previously).

We replaced the text em-dashes with commas, and for the date ranges we replaced them with en-dashes (`–`) or commas per standard resume typographic rules.

### Navigation Bar Redesign

The user requested to make the main navigation bar (`.nav-main`) more visually visible and prominent, preventing it from getting lost inside the hero section.
We executed the following changes:
1. **Glassmorphic Capsule Dock:** Transformed `.nav-main` in `css/sidebar.css` to a high-fidelity glassmorphic capsule using blur filters, dynamic border-colors, and subtle drop shadows.
2. **Pill Tab Interactions:** Styled nav links (`.nav-main a`) into sleek button-like capsule pills that scale slightly and shift up (`translateY(-1px)`) on hover.
3. **Brand-Colored Inline SVG Icons:** Embedded hand-crafted inline SVGs inside `index.html` and `projects/portradar/index.html`. We mapped theme-sensitive brand colors directly inside `sidebar.css` using clean attribute selectors (`a[href*="projects"] .nav-icon`, etc.), which keeps the markup clean and handles light/dark theme adjustments beautifully.
4. **Mobile Responsiveness:** Overrode layout rules under `@media (max-width: 600px)` so the navigation pill dock transitions smoothly into full-width touch-friendly segmented controls on mobile viewports.

### Favicon Integration

The user requested to make their orange duotone profile photo (`duotone.png`) the site favicon (website tab thumbnail image).
We executed the following changes:
1. **Favicon Injection:** Inserted the `<link rel="icon" type="image/png" href="...">` tag inside the `<head>` section of all 14 HTML files in the repository.
2. **Relative Paths Mapping:** Dynamically mapped the correct relative path to the new favicon based on the directory depth of each file (e.g. `favicon.png` for root, `../favicon.png` for `blog/*.html`, `../../favicon.png` for subfolders).
3. **Circular Glowing Asset:** Used AI image editing to crop the orange duotone profile photo into a perfect circle against a solid `#0A0A0F` background, surrounded by a soft glowing amber/orange light aura. Saved the generated asset as `favicon.png` in the root directory and updated all HTML references accordingly.

### Featured Project Highlight Removal

The user requested to remove the spotlight/featured highlighting from all projects.
We executed the following changes:
1. **Spotlight Removal:** Removed the `project-featured` class from the *MetaCode* article in `index.html` and `projects.html`, restoring all project cards to a uniform, clean layout.

### Mobile Hamburger Dropdown Menu

The user requested to replace the full-width mobile tag nav bar (which was wrapping to multiple lines) with a mobile-only top-left three-line hamburger menu that opens a glassmorphic dropdown panel when clicked.
We executed the following changes:
1. **Dynamic Menu Injection (js/theme.js):** Wrote a dynamic injection hook that automatically appends a `#mobile-menu-toggle` button (with a beautiful inline SVG hamburger icon) on pages that have `.nav-main`.
2. **Interactive Menu Toggle (js/theme.js):** Wired an event listener that toggles the `.nav-open` class on `.nav-main` (converting hamburger to an "X" close icon when open) and closes the menu automatically if the user clicks outside the nav area.
3. **Mobile CSS Overlay Styles (css/sidebar.css):**
   - Kept the mobile button hidden on desktop via `display: none;`.
   - On mobile viewports (`max-width: 600px`), positioned `#mobile-menu-toggle` fixed at `top: 20px; left: 24px;` (perfectly symmetrical to the theme toggle on the right).
   - Overrode `.nav-main` to be a fixed dropdown panel overlay with a heavy backdrop blur (`20px`), a elegant drop shadow, and smooth transitions (`opacity 0.25s`, `transform translateY(-12px)`).

### Favicon Background Removal

The user requested to make the favicon background transparent to remove the dark border/bar visible on lighter browser tab themes.
We executed the following changes:
1. **Radial Content Analysis:** Analyzed the horizontal luminance of the original 1024x1024 image, verifying perfect circle alignment and sizing at `cx=512`, `cy=512`.
2. **Alpha Channel Integration:** Reprocessed `favicon.png` using PIL to map its RGB channels to RGBA with an advanced transparency gradient.
3. **Perfect Edge Feathering:** Created an alpha transition fading from 255 (opaque) at radius 430px to 0 (transparent) at radius 455px, ensuring the glowing aura of the profile is preserved cleanly while corner black pixels are entirely keyed out.

### Portfolio Redesign for Prolific Shipped Projects
The user requested a design change to showcase the true scale of their projects portfolio (10+ active and hand-coded creations). We executed the following changes:
1. **Builder Dashboard Metric Row (index.html):** Added a high-end, glassmorphic dashboard underneath the hero section featuring clickable metrics: 10+ Shipped Projects, 5+ AI Collaborations, 20+ Articles Published, and 8+ Core Tech Stacks.
2. **Spotlight vs. Compact Vault Toggles (index.html):** Added interactive toggles to the homepage Projects header. "Spotlight" displays the existing 5 card layouts, while "Compact Vault" swaps instantly to a dense, spreadsheet-style directory list showing all 10 projects with custom status indicators and git links.
3. **Interactive Search & Filtering Deck (projects.html):** Restructured the entire projects page to present a unified, searchable list. Added a fuzzy search bar (querying names, tech tags, or descriptions) and custom topic filter pills (All, AI & ML, Dev Tools, Experiments & Games) with custom transition animations.
4. **High-Fidelity Styles (sidebar.css):** Wrote desktop, tablet, and mobile-responsive styles for the stats dashboard, capsule buttons, dense directory rows (with glowing margins on hover), and interactive text input fields. Checked alignment under light and dark themes.
5. **Interactive JavaScript Hooks (js/projects.js):** Wrote zero-dependency vanilla JavaScript logic to manage instantaneous state swaps, click boundaries, category filtering, and keyboard shortcuts (Focus with `/`).

### Layout Navigation & Stats Dashboard Refinements
Following a layout audit, we made the following updates to improve visual breathing room:
1. **Relocated Navigation Capsule (index.html):** Moved the `.nav-main` container HTML block to the very top of the `.hero-inner` flow (above the eyebrow and name header) so it starts above their name.
2. **Fixed Sticky Position (sidebar.css):** Maintained the high-fidelity centered floating fixed style (`top: 24px; left: 50%; transform: translateX(-50%);`), but added an extra `40px` top padding to the global page layout container (`padding: 100px 24px 60px;`) in both `sidebar.css` and `projects.html`. This cleanly pushes the content down so the floating nav bar sits perfectly in the top margin without overlapping your name.
3. **Removed Dashboard (index.html):** Removed the `builder-dashboard` metric panel HTML to keep the Hero section clean and focused.

### Static Layout Navigation & Single-Line Text Wrapping
To resolve layout overlapping and tab wrapping, we executed these finalized styles:
1. **Under-Name Layout Relocation (index.html):** Repositioned the `.nav-main` capsule to sit statically directly underneath the `<h1>Gaurab Bose</h1>` heading block in the Hero container flow.
2. **Reverted to Static Positioning (sidebar.css):** Reverted `.nav-main` from `fixed` back to relative flow (`display: inline-flex; margin-top: 20px; margin-bottom: 12px;`), restoring layout paddings on `.layout` back to a uniform `60px 24px;` in both `sidebar.css` and `projects.html`.
3. **Single-Line Text Wrapping (sidebar.css):** Applied `white-space: nowrap;` to `.nav-main a` links, completely resolving the vertical wrapping of the "Day Job" tab text.

### Compact Vault Mobile & Tablet Layout Redesign
The user reported that on mobile viewports, the Compact Vault directory looked broken (links wrapping to new lines, and descriptions/technologies hidden entirely).
We redesigned the layout under `@media (max-width: 768px)` to use **CSS Grid Template Areas**:
1. **Grid Template Areas:** Mapped `title year`, `tech tech`, and `status links` as the cell structure.
2. **Visible Technologies:** Unlocked `.c-tech` descriptions to be visible directly under the project title in an elegant, smaller size (`11.5px`), instead of hiding it.
3. **Side-by-Side Aligned Elements:** Positioned status badges on the bottom-left and Git/Blog links on the bottom-right of each row, saving vertical space and completely preventing any link wrapping or overlapping.
4. **Tabular Layout Hidden:** Hiding `.compact-header-row` since it transitions to a clean card directory layout.
5. **Automated Browser Verification:** Ran an automated browser check simulating a `375px` viewport and verified that the redesigned card layout fits flawlessly on mobile screens and matches all specified requirements.

### Navigation Capsule Relocation Below Subheader & Tab Wrapping Resolution
The user requested to move the static capsule navigation bar directly below their introduction subheader paragraph instead of directly under their name H1, to create a more clean typographic flow. They also wanted to resolve the wrapping of "Day Job" inside the navigation capsule.
We executed the following changes:
1. **Subheader Layout Relocation (index.html):** Moved `<nav class="nav-main">` to sit statically directly underneath `<p class="hero-statement">` ("Product Manager shaping...").
2. **Rhythm & Spacing Adjustments (sidebar.css):** Decreased `.hero-statement`'s bottom margin to `24px` and updated `.nav-main`'s desktop margins to `margin-top: 0; margin-bottom: 32px;` to balance the visual gaps perfectly.
3. **Enforced Nowrap Rule (sidebar.css):** Applied `white-space: nowrap !important;` on `.nav-main a` tabs to absolutely guarantee multi-word tabs (like "Day Job") sit on a single line under all viewport sizes.
4. **Updated Design Documentation (DESIGN.md):** Logged all updated structural hierarchies and layout specs into the design documentation.

### Compact Vault: Swapped Descriptions & Mobile Table Header Wrapping Resolution
Following additional user feedback, we executed the following refinements:
1. **Swapped Core Technologies with Descriptions:** Completely replaced the `.c-tech` (Core Technologies) column in `index.html` with `.c-desc` (Project Descriptions) and populated all 10 rows with full-sentence project descriptions.
2. **High-Fidelity Line Clamping:** Implemented CSS `-webkit-line-clamp: 2;` inside the `max-width: 768px` media query to truncate descriptions to a maximum of 2 lines on mobile/tablet, while retaining single-line ellipsis truncation on desktop to maintain spreadsheet density.
3. **Resolved Table Header Specificity Bug:** Relocated `.compact-header-row { display: none; }` below the `.compact-row` grid definition inside the `max-width: 768px` media query. This resolved the CSS ordering specificity conflict, successfully hiding table headers on smaller viewports instead of letting them display and wrap into multiple broken lines.

### MetaCode Building Badge Removal
The user finished building the *MetaCode* project and requested to remove its "Currently building" badges.
We executed the following changes:
1. **Spotlight Featured View (index.html):** Removed the `building-badge` element from MetaCode's project card header.
2. **Compact Vault View (index.html):** Removed the `building-badge` status pill from MetaCode's row.
3. **Full Projects List (projects.html):** Removed the `building-badge` element from MetaCode's list entry.
### RAG Summarizer and First Game Handcoded Badges
The user requested to tag both the RAG Paper Summarizer and the First Game projects as handcoded.
We executed the following changes:
1. **Projects Page (projects.html):** Added the `<span class="handcoded-badge">⌨️ Hand coded, Pre GenAI</span>` badge to both RAG Paper Summarizer and First Game cards under their headers.
2. **Spotlight Homepage List (index.html):** Added the `<span class="handcoded-badge">⌨️ Hand coded, Pre GenAI</span>` badge to the RAG Paper Summarizer project card header.
3. **Compact Vault Homepage Directory (index.html):** Updated the status column (`.c-status`) for both RAG Summarizer and First Game from `-` to `<span class="handcoded-badge">⌨️ Hand</span>` to match the other hand-coded projects.
